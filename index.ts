function calcularAreaT(base: number, altura: number): number { const area = (base * altura) / 2; 
return area; 
} 
const base = 7; 
const altura = 10; 
const area = calcularAreaT(base, altura); 
console.log(`El área del triángulo es: ${area}`);
